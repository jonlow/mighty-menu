import styled from "styled-components"
// import { device } from "breakpoints"
import { FlexCol } from "components/FlexGrid"

export const ExtendedFlexCol = styled(FlexCol)`
  h2 {
    border: 1px solid teal;
    width: 100%;
  }
`
